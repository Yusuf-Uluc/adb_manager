name: adb_manager
description: Application to manage ADB connections.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  system_tray: ^0.0.7
  bitsdojo_window: ^0.1.1+1
  process_run: ^0.12.2+2
  flutter_riverpod: ^0.14.0+3
  native_context_menu: ^0.2.0+3
  google_fonts: ^2.1.0
  shared_preferences: ^2.0.8
  url_launcher: ^6.0.12
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/tray_icon.png